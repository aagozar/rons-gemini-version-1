/* Tailwind v4 passa da questo plugin. Senza, gli stili non vengono
   generati e il sito esce tutto nero senza layout. */
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
